<view class="container">
  <navigator
    wx:for="{{componentsArray}}"
    wx:key="{{item.path}}"
    url="{{item.path}}"
    class="select-item-navigator"
  >
    <view>{{item.name}}</view>
  </navigator>
</view>














